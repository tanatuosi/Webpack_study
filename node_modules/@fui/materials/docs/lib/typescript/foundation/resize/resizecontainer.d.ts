import { Widget } from '@fui/core';
export declare class ResizableContainer extends Widget {
    static xtype: string;
    setMinSize(minWidth: number): void;
    setMaxSize(maxWidth: number): void;
    setResizable(resizable: boolean): void;
    reset(): void;
}
