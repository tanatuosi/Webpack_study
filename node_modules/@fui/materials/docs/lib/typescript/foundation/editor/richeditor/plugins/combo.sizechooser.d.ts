import { RichEditorAction } from '../bar/action.richeditor';
export declare class RichEditorSizeChooser extends RichEditorAction {
    static xtype: string;
    doAction(fontSize: number): void;
    activate(fontSize: number): void;
}
