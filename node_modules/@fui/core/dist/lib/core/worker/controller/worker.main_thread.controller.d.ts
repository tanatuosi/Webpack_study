import type { IWorkerOptions } from "../worker.core";
import { WorkerBaseController } from "./worker.controller";
export declare class WorkerMainThreadController extends WorkerBaseController {
    /**
     * 浏览器是否实现了 HTML 规范的 Worker Class
     */
    static hasWorkerClass: boolean;
    /**
     * 是否支持 new Worker, 默认为 Wroker Class 是否实现
     * 由于Blob url支持度问题  规避Safari 12以下版本
     */
    canNewWorker: boolean;
    /**
     * 主线程 new Worker 起始时刻
     */
    timeBeforeNewWorker: number;
    /**
     * 主线程 new Worker 完毕时刻
     */
    timeAfterNewWorker: number;
    constructor(options: IWorkerOptions);
    /**
     * 销毁 Worker 线程实例
     */
    terminate(): void;
    protected reportActionHandlerError(actionType: string, error: any): void;
    /**
     * 创建 Worker 线程实例
     */
    private newWorker;
}
