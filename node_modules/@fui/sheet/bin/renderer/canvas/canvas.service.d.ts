import { TextAlign } from '../../../types';
export declare function getRotatePosition({ rotate, x, y, width, height, textAlign, boundsWidth, baseline, }: {
    rotate: number;
    x: number;
    y: number;
    width: number;
    height: number;
    textAlign: TextAlign;
    boundsWidth: number;
    baseline: number;
}): {
    baseX: number;
    baseY: number;
    textBaseline: CanvasTextBaseline;
    textAlign: CanvasTextAlign;
};
