import { CellProps, CellStyle } from '../../../types';
export interface CellInterface {
    copyText: () => string;
    copyHtml: () => string;
}
export declare abstract class AbstractCell implements CellInterface {
    protected abstract _copyHtml(): string;
    protected abstract _copyText(): string;
    protected props: CellProps;
    protected style: CellStyle;
    constructor(props: CellProps);
    copyHtml(): string;
    copyText(): string;
}
