import { AbstractCell } from './abstract.cell';
export declare class HtmlCell extends AbstractCell {
    protected _copyHtml(): string;
    protected _copyText(): string;
    protected _renderContent(): Record<string, any>;
}
