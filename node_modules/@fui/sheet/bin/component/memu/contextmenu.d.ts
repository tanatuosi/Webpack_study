import { Element } from '../element';
import { MenuItem, OptionsType } from '../../../types';
/**
 * 右键单元格的菜单
 */
export declare class ContextMenu {
    el: Element;
    private clickHandler;
    private menuItems;
    constructor(menuItems?: MenuItem[]);
    show(x: number, y: number, options: OptionsType): void;
    hide(): void;
    bindHandler(handler: Function): void;
    tear(): void;
    private configItems;
    private buildItems;
}
