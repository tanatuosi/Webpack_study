import hotkeys from 'hotkeys-js';
let HotkeysManager = /** @class */ (() => {
    class HotkeysManager {
        constructor(props) {
            if (props && props.hotkeys) {
                Object.keys(props.hotkeys).forEach(key => {
                    this.bindHotkeys(key, props.hotkeys[key]);
                });
            }
        }
        bindHotkeys(keys, fn) {
            hotkeys(keys, fn);
        }
        unbindHotkeys(keys, fn) {
            hotkeys.unbind(keys, fn);
        }
    }
    HotkeysManager.KEY = {
        COPY: 'ctrl+c,command+c',
    };
    return HotkeysManager;
})();
export { HotkeysManager };
//# sourceMappingURL=hotkeys.js.map