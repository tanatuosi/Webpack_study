import { KeyHandler } from 'hotkeys-js';
import { HotkeysProps } from '../../types';
export declare class HotkeysManager {
    static KEY: {
        COPY: string;
    };
    constructor(props?: HotkeysProps);
    bindHotkeys(keys: string, fn: KeyHandler): void;
    unbindHotkeys(keys: string, fn: KeyHandler): void;
}
