import { AbstractWidget } from './widget';
export declare class Label extends AbstractWidget {
    private text;
    constructor(props: any);
    getText(): string;
    getRealWidth(): number;
}
