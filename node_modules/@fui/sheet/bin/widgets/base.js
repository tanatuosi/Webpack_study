const WidgetsMap = {};
function widget(key) {
    return function (target) {
        WidgetsMap[key] = target;
    };
}
function createWidget(config) {
    const { type = 'bi.label' } = config;
    if (WidgetsMap[type]) {
        return new WidgetsMap[type](config);
    }
    else {
        return null;
    }
}
export { widget, createWidget };
//# sourceMappingURL=base.js.map