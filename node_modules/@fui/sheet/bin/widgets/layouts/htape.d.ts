import { Cell } from 'types';
import { Layout } from './layout';
export declare class Htape extends Layout {
    items: any[];
    constructor(props: any);
    private digest;
    clickHandler: (e: Event, left: number, top: number, click: Cell) => any;
}
