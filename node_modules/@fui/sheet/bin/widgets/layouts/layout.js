import { AbstractWidget } from '../widget';
export class Layout extends AbstractWidget {
    getChildren() {
        return this.children;
    }
    getRealWidth() {
        return this.getChildren().reduce((sum, child) => child.getRealWidth() + sum, 0);
    }
}
//# sourceMappingURL=layout.js.map