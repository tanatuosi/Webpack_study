import { Cell } from 'types';
import { Layout } from './layout';
export declare class Absolute extends Layout {
    items: any[];
    constructor(props: any);
    protected digest(): void;
    clickHandler: (e: Event, left: number, top: number, click: Cell, relativePosition?: {
        x: number;
        y: number;
    }) => any;
}
