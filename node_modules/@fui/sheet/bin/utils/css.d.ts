import { Border, Line } from '../../types';
/**
 * 将字符串转成border
 * @param css css字符串
 */
export declare function parseBorder(css: string): Border;
/**
 * lineGradient计算
 * @param angle 弧度
 * @param w 宽
 * @param h 高
 */
export declare function getFitGradient(angle: number, w: number, h: number): Line;
