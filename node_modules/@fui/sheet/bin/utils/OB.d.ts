import { Event } from '../../types';
export declare class OB {
    private readonly eventMap;
    constructor();
    on(event: Event, cb: Function): void;
    off(event: Event, cb: Function): void;
    emit(event: Event, params?: any[]): void;
}
