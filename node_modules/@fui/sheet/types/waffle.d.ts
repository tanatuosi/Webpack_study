import { SheetElement } from './element';

export interface WaffleProps {
    width: number;
    height: number;
    freezeWidth: number;
    freezeHeight: number;
    totalWidth: number;
    totalHeight: number;
}

export interface Stick {
    tag?: string;
    id: string;
    el?: HTMLElement | Function | string;
    left: number;
    top: number;
    width: number;
    height: number;
    init?: (e: SheetElement, width: number, height: number, mount: Function | undefined, isInRange: boolean) => void;
    lazy?: boolean;
}
