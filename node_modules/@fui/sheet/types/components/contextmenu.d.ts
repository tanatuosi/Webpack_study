export declare type MenuEvent = 'copy';
export interface MenuItem {
    key: string;
    icon: string;
    label: string;
    accel: string;
}

export interface OptionsType {
    copy: boolean;
}
