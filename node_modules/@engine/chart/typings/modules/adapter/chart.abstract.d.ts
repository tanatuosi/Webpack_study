import { XHyperLink } from '@engine/link';
export declare abstract class AbstractReportChart extends BI.Widget {
    static EVENT_LOADED: string;
    protected chart: any;
    protected chartAttr: any;
    props: {
        chartAttrGetter: () => Promise<{
            chartAttr: any;
            chartID: string;
        } | {
            errorCode: string;
            errorMsg: string;
        }>;
        autoSize: boolean;
        instantiateXHyperLinks: (javaScriptGroup: XHyperLink[]) => import("@engine/link").AbstractLink[];
        wScale: number;
        hScale: number;
        fontScale: number;
    };
    /**
     * 刷新，重绘但不重新请求数据
     */
    abstract refresh(): void;
    /**
     * 重新请求数据
     */
    abstract reload(): void;
    /**
     * 修改图表参数，用于联动
     * @param params 参数
     */
    abstract changeParameter(params: {
        [key: string]: any;
    }): void;
    /**
     * resize接口
     * @param wScale 宽缩放比例
     * @param hScale 高缩放比例
     * @param fontScale 文字缩放比例
     */
    abstract resize(wScale: number, hScale: number, fontScale: number): void;
    /**
     * 清除图表接口
     */
    abstract remove(): void;
    /**
     * 销毁时的生命周期钩子
     */
    destroy(): void;
}
