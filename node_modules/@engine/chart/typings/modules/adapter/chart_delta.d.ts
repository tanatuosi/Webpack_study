import { ReportChartGamma } from './chart_gamma';
export declare class ReportChartDelta extends ReportChartGamma {
    static xtype: string;
    static VERSION: string;
    static BUILD_TIME: string;
    resize(wScale: number, hScale: number, fontScale: number): void;
    protected enforceChartAttr(): void;
    refresh(): void;
    render(): {
        type: string;
    };
}
