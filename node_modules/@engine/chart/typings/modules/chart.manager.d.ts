import { WidgetManager } from '@engine/utils';
import { AbstractReportChart } from './adapter/chart.abstract';
export declare class ChartManager extends WidgetManager<AbstractReportChart> {
    register(instance: AbstractReportChart, chartId: string): void;
    destroy(): void;
    refresh(): void;
}
export declare const chartManager: ChartManager;
