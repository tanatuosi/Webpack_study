import { instantiateXHyperLinks } from '@engine/link';
export class AbstractReportChart extends BI.Widget {
    constructor() {
        super(...arguments);
        this.props = {
            chartAttrGetter: async () => ({}),
            autoSize: true,
            instantiateXHyperLinks: (javaScriptGroup) => instantiateXHyperLinks(javaScriptGroup),
            wScale: 1,
            hScale: 1,
            fontScale: 1,
        };
    }
    /**
     * 销毁时的生命周期钩子
     */
    destroy() {
        this.remove();
    }
}
AbstractReportChart.EVENT_LOADED = 'EVENT_LOADED';
//# sourceMappingURL=chart.abstract.js.map