var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { requestGet, requestPost } from '@engine/crud';
/**
 * 获取图表数据
 * @param chartID 图表ID
 */
export function fetchChartData(opt) {
    const { chartID, widgetName = '' } = opt, args = __rest(opt, ["chartID", "widgetName"]);
    return requestGet('chart/data', {
        params: Object.assign({ chartID, ecName: widgetName }, args),
    });
}
/**
 * 获取超链信息
 * @param data 传输数据
 */
export function fetchChartHyperLinks(data) {
    return requestPost('chart/calculate/hyperlink', {
        data,
    });
}
//# sourceMappingURL=chart.crud.js.map