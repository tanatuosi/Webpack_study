import { WidgetManager } from '@engine/utils';
export class ChartManager extends WidgetManager {
    register(instance, chartId) {
        super.register(instance, chartId.toUpperCase());
    }
    destroy() {
        this.getWidgets()
            .forEach(chart => {
            chart.remove();
        });
        super.destroy();
    }
    refresh() {
        this.getWidgets()
            .forEach(chart => {
            chart.refresh();
        });
    }
}
export const chartManager = new ChartManager();
//# sourceMappingURL=chart.manager.js.map