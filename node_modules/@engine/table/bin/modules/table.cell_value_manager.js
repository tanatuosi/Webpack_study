import { WidgetManager } from '@engine/utils';
export class CellCacheManager extends WidgetManager {
    constructor() {
        super(...arguments);
        this.updatedCellValueCache = [];
    }
    addCellCache(cellData) {
        this.updatedCellValueCache.push(cellData);
    }
    getCellCache() {
        return this.updatedCellValueCache;
    }
    clearCellCache() {
        this.updatedCellValueCache = [];
    }
    /**
     * 在resize时，不能够去掉cache信息，否则失焦时会无法将值更新上去
     * 修复 https://work.fineres.com/browse/REPORT-73661 删除
     */
    update() { }
    /**
     * 清除字典
     */
    clear() {
        this.clearCellCache();
        super.clear();
    }
}
CellCacheManager.NAME = 'CELL_CACHE';
//# sourceMappingURL=table.cell_value_manager.js.map