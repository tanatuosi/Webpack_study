var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { HEIGHT, ParameterCheckboxGroup, WIDTH } from '@engine/controller';
import { shortcut } from '@engine/core';
import { AbstractParameterCellControllerWidget } from '../cell_controller.abstract';
import { fetchCellGroupControlAttr } from '../cell_controller.crud';
let CellCheckboxGroup = class CellCheckboxGroup extends AbstractParameterCellControllerWidget {
    constructor() {
        super(...arguments);
        this.props = {
            width: WIDTH,
            height: HEIGHT,
            fontSize: 12,
            reportIndex: 0,
            position: {
                row: 0,
                column: 0,
            },
            xlisteners: [],
            baseCls: '',
        };
    }
    render() {
        const _a = this.options, { xlisteners, position, reportIndex, baseCls } = _a, opts = __rest(_a, ["xlisteners", "position", "reportIndex", "baseCls"]);
        return Object.assign(Object.assign({}, opts), { type: ParameterCheckboxGroup.xtype, ref: (ref) => {
                this.group = ref;
            }, listeners: xlisteners, controlAttrFetcher: fetchCellGroupControlAttr, extraOptsGetter: () => {
                return {
                    location: Object.assign(Object.assign({}, position), { reportIndex }),
                };
            }, beforeRender(cb) {
                this.updateControlAttr()
                    .then(() => cb());
            } });
    }
    // 复写父类不触发afterinit事件
    mounted() { }
    getValue() {
        return this.group.getValue();
    }
    postGetValidation() {
        return this.group.postGetValidation();
    }
    preGetValidation() {
        return this.group.preGetValidation();
    }
    async setControllerValue(v) {
        await this.group.setControllerValue(v);
    }
    reset() {
        this.group.reset();
    }
    postValidateAndToast() {
        return this.group.postValidateAndToast();
    }
    setInteractValue(v) {
        this.group.setInteractValue(v);
    }
    reloadControlAttr() {
        this.group.reloadControlAttr();
    }
    setStyle() { }
};
CellCheckboxGroup.xtype = 'report.main.table_cell.controller.checkbox_group';
CellCheckboxGroup = __decorate([
    shortcut()
], CellCheckboxGroup);
export { CellCheckboxGroup };
//# sourceMappingURL=cell_controller.checkbox_group.js.map