import { AbstractParameterCellControllerWidget } from '../cell_controller.abstract';
/**
 * 单元格控件高阶类，用于封装后的单元格控件
 */
export class AbstractCellControllerHoc extends AbstractParameterCellControllerWidget {
    // 单元格控件不存在直接联动
    setInteractValue() { }
    /**
     * 创建水印json
     */
    createWatermark() {
        const value = this.options.el.value;
        return {
            el: {
                type: BI.Label.xtype,
                invisible: this.checkValueNotEmpty(value),
                value: this.options.el.watermark,
                textAlign: 'left',
                cls: 'bi-water-mark',
                ref: (ref) => {
                    this.watermark = ref;
                },
            },
            top: 1,
            left: 1,
            right: 1,
            bottom: 1,
        };
    }
    checkValueNotEmpty(value) {
        return !BI.isEmptyString(value) && BI.isNotNull(value);
    }
}
//# sourceMappingURL=cell_controller_hoc.abstract.js.map