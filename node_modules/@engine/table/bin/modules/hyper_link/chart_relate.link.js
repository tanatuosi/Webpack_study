import { AbstractLink } from '@engine/link';
import { CellChartManager } from '../table.cell_chart_manager';
export class ChartRelateLink extends AbstractLink {
    constructor(props) {
        var _a;
        super(props);
        this.silent = true;
        this.parameters = props.parameters;
        this.managersGetter = (_a = props.external) === null || _a === void 0 ? void 0 : _a.managersGetter;
        this.relateCCName = props.relateCCName;
    }
    _excute() {
        var _a;
        const params = this.parameters.reduce((params, { value, name }) => {
            params[name.toUpperCase()] = value;
            return params;
        }, {});
        const cellChartManager = ((_a = this.managersGetter) === null || _a === void 0 ? void 0 : _a.call(this).get(CellChartManager.NAME));
        if (!cellChartManager) {
            return;
        }
        cellChartManager.link(params, this.relateCCName);
    }
}
//# sourceMappingURL=chart_relate.link.js.map