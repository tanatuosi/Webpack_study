import { TableRowColumn } from './table.api_row_column.abstract';
/**
 * 列对象
 */
export class TableColumn extends TableRowColumn {
    /**
     * 获取列对象的统一横坐标
     * @returns 坐标
     */
    getUnifiedPosition() {
        return {
            x: this.unifiedPosition,
        };
    }
    /**
     * 设置列宽
     * @param width 列宽
     */
    setWidth(width) {
        this.size = width;
        this.externalContext.emitCellEvent('CELL_SET_SIZE', {
            x: undefined,
            y: this.index - 1,
            height: undefined,
            width,
        });
    }
    /**
     * 获取列宽
     * @returns 列宽
     */
    getWidth() {
        return this.size;
    }
    /**
     * 获取第row行的单元格
     * @param row 行
     * @returns 单元格
     */
    getCell(row) {
        return this.externalContext.getCell(this.index, row);
    }
}
//# sourceMappingURL=table.api_column.js.map