import { CellControllerManager } from '../table.cell_controller_manager';
import { AbstractCellHandler } from './handler.abstract';
export class WidgetInactiveHandler extends AbstractCellHandler {
    constructor() {
        super(...arguments);
        this.eventNames = ['CELL_DOUBLE_CLICK', 'CELL_CLICK'];
        this.name = 'CELL_INACTIVE_WIDGET';
        this.merger = 'uniqe';
    }
    async action({ id }) {
        const cellControllerManager = (this.externalContext.getStickerManagers().get(CellControllerManager.NAME));
        if (!cellControllerManager) {
            return;
        }
        cellControllerManager
            .getWidgetsMap()
            .forEach((controller, name) => {
            name !== id && controller.inactive();
        });
    }
}
//# sourceMappingURL=handler.inactive_controller.js.map