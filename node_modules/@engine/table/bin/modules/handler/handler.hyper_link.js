import { HyperLinkClickHandler } from '@engine/link';
import { AbstractCellHandler } from './handler.abstract';
export class HyperLinkCellClickHandler extends AbstractCellHandler {
    constructor() {
        super(...arguments);
        this.eventNames = ['CELL_CLICK'];
        this.merger = '';
        this.name = 'CELL_HYPERLINK';
        this.handler = new HyperLinkClickHandler(this.externalContext.attach);
    }
    action({ e, hyperLinks }) {
        this.handler.handle(e, hyperLinks);
    }
}
//# sourceMappingURL=handler.hyper_link.js.map