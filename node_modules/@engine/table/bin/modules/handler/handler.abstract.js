export class AbstractCellHandler {
    constructor(externalContext) {
        this.externalContext = externalContext;
    }
    get tableData() {
        return this.externalContext.getTableData();
    }
    getCellById(id) {
        if (!id)
            return;
        const pageResults = this.tableData;
        let pageCellData;
        pageResults.some(rowData => rowData.some(cellData => {
            if (cellData.id === id) {
                pageCellData = cellData;
                return true;
            }
            return false;
        }));
        if (!pageCellData) {
            return;
        }
        return pageCellData;
    }
}
//# sourceMappingURL=handler.abstract.js.map