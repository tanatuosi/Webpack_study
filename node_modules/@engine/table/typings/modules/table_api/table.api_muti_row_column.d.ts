import { CellSetStyleArgs, CellTooltipArgs, TableApiContext, XCellPosition } from '../table.types';
import { TableRowColumn } from './table.api_row_column.abstract';
/**
 * 多行列对象
 */
export declare class MutiTableRowColumn {
    private tableRowColumns;
    private externalContext;
    constructor(externalContext: TableApiContext);
    /**
     * 设置所有行对象行高
     * @param height 行高
     */
    setHeight(height: number): void;
    /**
     * 获取所有行对象行高
     * @returns 行高
     */
    getHeight(): number[];
    /**
     * 设置所有列宽
     * @param width 列宽
     */
    setWidth(width: number): void;
    /**
     * 获取所有列宽
     * @returns 列宽
     */
    getWidth(): number[];
    /**
     * 获取所选行/列单元格对象
     * @param index 索引
     * @returns 单元格
     */
    getCell(index: number): (import("./table.api_cell").TableCell | undefined)[];
    /**
     * 获取所在行/列
     * @returns 行/列
     */
    getIndex(): number[];
    /**
     * 设置所选行列样式
     */
    setStyle(opts: CellSetStyleArgs): void;
    /**
     * 展示所选行列悬浮提示
     * @param opts 配置项
     */
    showToolTip(opts: CellTooltipArgs): void;
    /**
     * 生成所需的带索引结构
     * @returns 多行列对象
     */
    createMultipleApi(type: string, position: XCellPosition): ArrayLike<TableRowColumn>;
}
