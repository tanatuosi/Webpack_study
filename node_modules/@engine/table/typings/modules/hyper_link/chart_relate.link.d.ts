import { AbstractLink, HyperLinkParameter, HyperLinkProps } from '@engine/link';
import { WidgetManager } from '@engine/utils';
export declare class ChartRelateLink extends AbstractLink {
    private managersGetter?;
    private relateCCName;
    protected parameters: HyperLinkParameter[];
    silent: boolean;
    constructor(props: ChartRelateProps);
    protected _excute(): void;
}
export interface ChartRelateProps extends HyperLinkProps {
    chartId: string;
    parameters: HyperLinkParameter[];
    relateCCName: string;
    external?: {
        managersGetter: () => Map<string, WidgetManager<any>>;
    };
}
