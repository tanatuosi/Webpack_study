import { Widget } from '@fui/core';
import { Bounds } from '@fui/sheet/types';
import { WidgetManager } from '@engine/utils';
/**
 * 管理所有报错单元格的标签
 */
export declare class CellErrorTipManager extends WidgetManager<Widget> {
    static NAME: string;
    destroy(): void;
    update(): void;
    /**
     * 清除字典
     */
    clear(): void;
    /**
     * 调整错误消息的显示位置以及宽度
     * @param id
     * @param cell
     * @param currentViewPort
     * @returns
     */
    adjustErrorTip(id: string, cell: Bounds, currentViewPort: {
        left: number;
        top: number;
        right: number;
        bottom: number;
        width: number;
        height: number;
    }): void;
}
