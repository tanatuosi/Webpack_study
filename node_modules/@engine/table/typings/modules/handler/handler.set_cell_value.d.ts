import { XCellValue } from '../table.types';
import { AbstractCellHandler } from './handler.abstract';
export declare class CellSetValueHandler extends AbstractCellHandler {
    eventNames: string[];
    name: string;
    merger: string;
    action({ value, x, y }: TableCellSetValueHandler): Promise<boolean>;
}
export declare type TableCellSetValueHandler = Parameters<AbstractCellHandler['action']>[0] & {
    value: XCellValue['cv'];
};
