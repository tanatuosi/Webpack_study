import { Cell } from '@fui/sheet/types';
import { PageResult } from '../table.types';
import { HandlerExternalContext } from './handler';
export declare abstract class AbstractCellHandler {
    protected externalContext: HandlerExternalContext;
    /**
     * Handler的名称
     */
    abstract name: string;
    /**
     * 触发事件
     */
    abstract eventNames: string[];
    /**
     * 类型
     * 'uniqe' 唯一，存在即不会再被注册;
     * 'covered' 覆盖，新的会覆盖旧的;
     * '' 正常注册;
     */
    abstract merger: string;
    abstract action(args: {
        e?: Event;
        x: number;
        y: number;
        cell: Cell;
        [key: string]: any;
    }): void | Promise<any>;
    constructor(externalContext: HandlerExternalContext);
    protected get tableData(): PageResult[][];
    protected getCellById(id?: string): PageResult | undefined;
}
