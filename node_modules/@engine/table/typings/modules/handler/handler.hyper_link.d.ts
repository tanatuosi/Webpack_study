import { AbstractLink } from '@engine/link';
import { AbstractCellHandler } from './handler.abstract';
export declare class HyperLinkCellClickHandler extends AbstractCellHandler {
    eventNames: string[];
    merger: string;
    name: string;
    private handler;
    action({ e, hyperLinks }: Parameters<AbstractCellHandler['action']>[0] & {
        hyperLinks: AbstractLink[];
    }): void;
}
