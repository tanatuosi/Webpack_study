import { PageResult, XCellPosition } from '../table.types';
/**
 * 将坐标转换为key
 * @param x 横坐标
 * @param y 纵坐标
 */
export declare function encodeCacheKey(x: number, y: number): string;
/**
 * 将key转换为坐标
 * @param key key值
 */
export declare function decodeCacheKey(key: string): {
    x: number;
    y: number;
};
/**
 * 修改单元格内容
 * @param raw 原始值
 * @param cells 修改数组
 * @param handler 处理方式
 */
export declare function alterRawTable<T>(raw: PageResult[][], cells: {
    x: number;
    y: number;
    alterer: T;
}[], handler: (raw: PageResult, alterer: T) => void): void;
/**
 * 根据x y找到cell
 * @param x x坐标
 * @param y y坐标
 */
export declare function findCellByPosition(raw: PageResult[][], x: number, y: number): PageResult | undefined;
/**
 * 根据x y找到同一行的cells
 * @param x x坐标
 * @param y y坐标
 */
export declare function findCellLineByPosition(raw: PageResult[][], y: number): PageResult[];
/**
 * 根据当前坐标获取满足条件的cells
 * @param raw 原始值
 * @param position 坐标
 * @param filterOperation 过滤条件
 */
export declare function findValidCellData(raw: PageResult[][], position: XCellPosition, filterOperation: (cell: PageResult, position: XCellPosition) => boolean): PageResult[];
