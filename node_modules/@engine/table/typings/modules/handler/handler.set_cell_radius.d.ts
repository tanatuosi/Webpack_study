import { TableRadiusRangeArgs } from '../table.types';
import { AbstractCellHandler } from './handler.abstract';
/**
 * 设置单元格弧度
 */
export declare class CellSetRadiusHandler extends AbstractCellHandler {
    eventNames: string[];
    name: string;
    merger: string;
    action({ radius, range, single }: TableCellSetRadiusHandler): void;
    /**
    * 设置单元格圆角
    * @param cell 单元格
    * @param radius 圆角
    * @param direction 位置
    */
    private setCellRadius;
}
export declare type TableCellSetRadiusHandler = Parameters<AbstractCellHandler['action']>[0] & {
    radius: number;
    range?: TableRadiusRangeArgs;
    single?: boolean;
};
