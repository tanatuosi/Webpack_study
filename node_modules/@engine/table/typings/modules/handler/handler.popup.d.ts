import { CellSetStyleArgs } from '../table.types';
import { HandlerExternalContext } from './handler';
import { AbstractCellHandler } from './handler.abstract';
export declare class PopupHandler extends AbstractCellHandler {
    name: string;
    eventNames: string[];
    merger: string;
    private combo;
    private textLabel;
    private position;
    private scopeType;
    private attach;
    private useCellValue;
    constructor(externalInterface: HandlerExternalContext, config: popupConfig);
    private createPopupStyle;
    action({ cell, e }: Parameters<AbstractCellHandler['action']>[0]): void;
    private doAction;
}
interface popupConfig {
    position: {
        x?: number;
        y?: number;
    };
    scopeType: 'table' | 'cell' | 'rowcolumn';
    body: string;
    header: string | undefined;
    image?: string;
    style?: CellSetStyleArgs;
}
export {};
