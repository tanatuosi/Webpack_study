import { AbstractCellHandler } from './handler.abstract';
export declare class WidgetActiveHandler extends AbstractCellHandler {
    eventNames: string[];
    name: string;
    merger: string;
    action({ id }: Parameters<AbstractCellHandler['action']>[0] & {
        id: string;
    }): Promise<void>;
}
