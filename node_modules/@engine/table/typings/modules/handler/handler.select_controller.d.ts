import { AbstractCellHandler } from './handler.abstract';
export declare class CellSelectHandler extends AbstractCellHandler {
    eventNames: string[];
    name: string;
    merger: string;
    action({ id, cell }: Parameters<AbstractCellHandler['action']>[0] & {
        id: string;
    }): Promise<void>;
}
