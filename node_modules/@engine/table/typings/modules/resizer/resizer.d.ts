import { ReportFitAttr } from '../table.types';
import { IAbstractResizerConstructorArgs } from './resizer.abstract';
import { NoneResizer } from './resizer.none';
/**
 * 对外使用的provider
 */
declare class ReportTableResizerProvider {
    static xtype: string;
    private resizeModes;
    $get(): {
        new (): {
            setResizer(type: number, newResizeMode: typeof NoneResizer): void;
            /**
             *
             * @param type 1：宽度自适应，2：双向自适应，3：不自适应
             */
            getResizer(type: number, opts?: IAbstractResizerConstructorArgs | undefined): NoneResizer;
        };
    };
    addResizer(type: number, value: {
        [key: string]: any;
    }): void;
}
export declare const resizerProvider: InstanceType<ReturnType<ReportTableResizerProvider['$get']>>;
export interface ResizeOption {
    tableWidth: number;
    tableHeight: number;
    paperWidth: number;
    paperHeight: number;
    fitAttr: ReportFitAttr;
    hasFrozen?: boolean;
}
export {};
