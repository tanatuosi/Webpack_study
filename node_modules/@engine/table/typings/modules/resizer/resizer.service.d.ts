import { CellType, TableStickerOpt } from '../table.types';
export declare const fontScaleAttributeName = "data-font-scale";
/**
 * 对sticker进行自适应
 * @param rate 字体缩放比例
 * @param stickers stickers
 */
export declare function resizeStickerExtension(rate: number, extension: NonNullable<TableStickerOpt['extension']>): NonNullable<TableStickerOpt['extension']>;
/**
 * 移除横向作为边距的宽度
 */
export declare function removeColsAdjust(cols: number[]): number[];
/**
 * 移除纵向作为边距的宽度
 */
export declare function removeRowsAdjust(rows: number[]): number[];
/**
 * 根据缩放比例调整宽高，抹平浮点数
 * @param sizeList
 * @param rate
 * @returns
 */
export declare function adjustSizeByRate(sizeList: number[], rate: number): number[];
export declare function resize(max: number, sizes?: number[], scale?: number): number[];
export declare function resizeFont(rate: number, minFontSize?: number, cells?: CellType[][]): void;
/**
 * 处理单元格尺寸
 * 如果格子尺寸相加仅仅比报表块大一点点(不大于2px)
 * 则调整一下尺寸以至于不要出现滚动条
 */
export declare function adjustSize(max: number, sizes?: number[]): number[];
