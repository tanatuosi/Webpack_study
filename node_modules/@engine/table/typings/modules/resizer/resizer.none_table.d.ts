import { SheetOptions } from '@fui/sheet/types';
import { ResizeOption } from './resizer';
import { AbstractResizer } from './resizer.abstract';
/**
 * 不调整 + 移除报表间距
 */
export declare class NoneTableResizer extends AbstractResizer {
    isAdaptive: boolean;
    resize(sheet: SheetOptions, opt: ResizeOption): SheetOptions;
}
