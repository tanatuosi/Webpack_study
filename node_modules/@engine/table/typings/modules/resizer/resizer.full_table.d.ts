import { SheetOptions } from '@fui/sheet/types';
import { ResizeOption } from './resizer';
import { AbstractResizer } from './resizer.abstract';
/**
 * 不自动调整的双向自适应
 * 按照表格宽度缩放不会按照paper宽度去缩放
 * 字体缩放大小跟随横纵最小比例
 */
export declare class FullTableResizer extends AbstractResizer {
    resize(data: SheetOptions, opt: ResizeOption): SheetOptions;
}
