import { AbstractResizer } from './resizer.abstract';
import { ResizeOption } from './resizer';
import { SheetOptions } from '@fui/sheet/types';
/**
 * 不自适应，该咋咋滴
 */
export declare class NoneResizer extends AbstractResizer {
    isAdaptive: boolean;
    resize(sheet: SheetOptions, _opt: ResizeOption): SheetOptions;
}
