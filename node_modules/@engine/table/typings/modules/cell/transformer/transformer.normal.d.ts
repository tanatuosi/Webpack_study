import { TableExternalContext } from '../../table.cell_generator';
import { PageResult, XCellValue } from '../../table.types';
export declare class NormalCellValueTransformer {
    protected externalInterface: TableExternalContext;
    protected cellData: PageResult;
    constructor(externalInterface: TableExternalContext, cellData: PageResult);
    setValue(cellValue: XCellValue, update?: {
        cv: NonNullable<XCellValue['cv']>;
        pv?: string;
    }): Promise<string>;
    /**
     * 获取依赖键值对
     * @param dependence 依赖数组
     * @returns 依赖键值对
     */
    protected getDependenceValue(dependence: string[]): {
        [key: string]: any;
    };
}
