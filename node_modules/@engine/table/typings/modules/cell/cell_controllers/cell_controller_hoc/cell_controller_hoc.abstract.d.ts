import { Label } from '@fui/core';
import { AbstractParameterCellControllerWidget } from '../cell_controller.abstract';
/**
 * 单元格控件高阶类，用于封装后的单元格控件
 */
export declare abstract class AbstractCellControllerHoc extends AbstractParameterCellControllerWidget {
    props: {
        baseCls: string;
        el: {
            [key: string]: any;
        };
        width: number;
        height: number;
        fontSize: number;
        slient: boolean;
        shouldHideSelectFrame: () => boolean;
    };
    abstract active(): void;
    abstract inactive(): void;
    abstract isActive(): boolean;
    abstract setText(v: string): void;
    abstract setSeleted(v: boolean): void;
    abstract hideHightlightBorder(): void;
    setInteractValue(): void;
    protected watermark?: Label;
    /**
     * 创建水印json
     */
    protected createWatermark(): {
        el: {
            type: string;
            invisible: boolean;
            value: any;
            textAlign: string;
            cls: string;
            ref: (ref: Label) => void;
        };
        top: number;
        left: number;
        right: number;
        bottom: number;
    };
    protected checkValueNotEmpty(value: any): boolean;
}
