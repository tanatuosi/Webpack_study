import { ParameterEmpty } from '@engine/controller';
declare class CellControllerProvider {
    private controllerDict;
    /**
     * EVENT_CHANGE对应的绑定事件，即A事件后必发EVENT_CHANGE
     */
    private controllerEventChangeDict;
    getType(key: string): string;
    isDirect(key: string): boolean;
    set(key: string, value: string, direct?: boolean): void;
    getImpl(): typeof ParameterEmpty;
    getTextTransformer(type: string): (v: string) => string;
    isSlient(key: string): boolean;
    getEventChangeName(type: string): string | undefined;
}
export declare const cellControllerProvider: CellControllerProvider;
export {};
