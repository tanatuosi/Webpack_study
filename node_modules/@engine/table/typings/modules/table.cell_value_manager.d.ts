import { WidgetManager } from '@engine/utils';
import { PageResult } from './table.types';
export declare class CellCacheManager extends WidgetManager<Widget> {
    static NAME: string;
    private updatedCellValueCache;
    addCellCache(cellData: PageResult): void;
    getCellCache(): PageResult[];
    private clearCellCache;
    /**
     * 在resize时，不能够去掉cache信息，否则失焦时会无法将值更新上去
     * 修复 https://work.fineres.com/browse/REPORT-73661 删除
     */
    update(): void;
    /**
     * 清除字典
     */
    clear(): void;
}
