import { requestPost } from '@engine/crud';
/**
 * 数据入库
 */
export function dbCommit(opt, parameters = {}) {
    return requestPost('db/commit', {
        data: {
            __parameters__: Object.assign(Object.assign({}, opt), parameters),
        },
        encoder: v => encodeURIComponent(v),
    });
}
//# sourceMappingURL=manager.crud.js.map