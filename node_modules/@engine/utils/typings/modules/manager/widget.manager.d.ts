import { CoreWidgetManager } from '@engine/core';
import { Widget } from '@fui/core';
import { dbCommitConfig } from './manager.crud';
export declare class WidgetManager<T extends Widget> extends CoreWidgetManager<T> {
    /**
     * 数据入库
     */
    dbCommit(opt: dbCommitConfig): void;
}
