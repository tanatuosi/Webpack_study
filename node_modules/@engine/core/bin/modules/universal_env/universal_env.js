import { envProvider } from '../env/env';
export class UniversalEnvProvider {
    get ReqPrefix() {
        var _a;
        return ((_a = this.env) === null || _a === void 0 ? void 0 : _a.getReqPrefix()) || envProvider.ReqPrefix;
    }
    use(Env) {
        this.env = new Env();
    }
    assign(env) {
        this.env = env;
    }
}
export const universalEnvProvider = new UniversalEnvProvider();
//# sourceMappingURL=universal_env.js.map