import { Model } from '../../core.service';
import type { ErrorResult } from './errortip';
export declare enum ErrorTipTab {
    SOLUTION = "solution",
    EXCEPTION = "exception"
}
export declare class ErrorTipModel extends Model {
    static xtype: string;
    state(): {
        showIndex: ErrorTipTab;
        errorResult: ErrorResult;
    };
    actions: {
        changeTabPanel: (tab: ErrorTipTab) => void;
        setErrorResult: (errorResult: ErrorResult) => void;
    };
}
