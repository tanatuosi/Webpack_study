import { DateType } from './report_date';
export declare function parseDateTime(date: DateType, format?: string): {
    date: Date;
    isLegal: boolean;
};
/**
 * 将格式转换为fineui控件接收的格式
 * @param format 格式
 */
export declare function transformFormat(format: string): string;
/**
 * 将format字符串转换为正则表达式
 * @param formatStr
 */
export declare function format2Regex(formatStr: string): string;
/**
 * 检验日期格式和format是否匹配
 * @param date
 * @param format
 */
export declare function checkDateValid(date: string, format: string): boolean;
export declare function getFormatType(format: string): {
    isDateFormat: boolean;
    isTimeFormat: boolean;
    isYearFormat: boolean;
};
/**
 * 判断日期字符串是否需要校验
 */
export declare function needFormat(dateStr: string): boolean;
export declare function replaceDateStr(dateStr: string): string;
