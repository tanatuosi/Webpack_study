/**
 * 校验值
 * @param v 控件值
 * @param opts 校验条件
 * @param opts.allowBlank 是否允许为空
 * @param opts.regex 正则校验字符串
 */
export declare function validateParamPassword(v: string, { allowBlank, regex }: {
    allowBlank: boolean;
    regex: string;
}): boolean;
/**
 * 根据配置信息
 * @param v 控件值
 * @param opts 配置参数
 * @param opts.allowBlank 是否允许为空
 * @param opts.errorMsg 空白报错信息
 * @param opts.regErrorMsg 校验报错信息
 */
export declare function getParamPasswordErrorMsg(v: string, { allowBlank, errorMsg, regErrorMsg }: {
    allowBlank: boolean;
    errorMsg: string;
    regErrorMsg: string;
}): string;
