/**
 * 校验值
 * @param v 控件值
 * @param opts 校验条件
 * @param opts.allowBlank 是否允许为空
 * @param opts.regex 正则校验字符串
 */
export function validateParamPassword(v, { allowBlank, regex }) {
    if (v === '' && !allowBlank) {
        return false;
    }
    if (v === '' && allowBlank) {
        return true;
    }
    return new RegExp(regex).test(v);
}
/**
 * 根据配置信息
 * @param v 控件值
 * @param opts 配置参数
 * @param opts.allowBlank 是否允许为空
 * @param opts.errorMsg 空白报错信息
 * @param opts.regErrorMsg 校验报错信息
 */
export function getParamPasswordErrorMsg(v, { allowBlank, errorMsg, regErrorMsg }) {
    if (v === '' && !allowBlank) {
        return errorMsg;
    }
    return regErrorMsg;
}
//# sourceMappingURL=password.service.js.map