import { AbstractLink } from './links/abstract.link';
/**
 * Hyperlink点击处理，表格、图表通用
 */
export declare class HyperLinkClickHandler {
    private popup;
    private hyperLinks;
    private attach;
    constructor(attach: (widget: {
        [key: string]: any;
    }) => void);
    handle(e: Event, _hyperLinks: AbstractLink[]): void;
}
