import { AbstractTarget } from './target.abstract';
export declare class SelfTarget extends AbstractTarget {
    private byPost?;
    private parameter?;
    constructor(target: string, url: string, op: {
        byPost?: boolean;
        parameter?: string;
    });
    handle(): void | Window | null;
}
