import { AbstractTarget } from './target.abstract';
export declare class DefaultTarget extends AbstractTarget {
    protected options: any;
    constructor(target: string, url: string, op: any);
    handle(): Window | null;
}
