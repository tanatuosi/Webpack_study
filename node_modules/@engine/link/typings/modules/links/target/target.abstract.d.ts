export declare abstract class AbstractTarget {
    protected target: string;
    protected url: string;
    constructor(target: string, url: string, _op: {
        [key: string]: any;
    });
    abstract handle(): any;
}
