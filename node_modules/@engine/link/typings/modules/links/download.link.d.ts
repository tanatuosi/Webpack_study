import { AbstractLink, HyperLinkProps } from './abstract.link';
export declare class DownloadLink extends AbstractLink {
    protected id: string;
    constructor(props: DownloadProps);
    protected _excute(): void;
}
export interface DownloadProps extends HyperLinkProps {
    id: string;
}
