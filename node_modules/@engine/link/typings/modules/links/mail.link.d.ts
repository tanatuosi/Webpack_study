import { AbstractLink, HyperLinkProps } from './abstract.link';
export declare class MailLink extends AbstractLink {
    protected mailLinkProps: MailLinkProps;
    constructor(props: MailLinkProps);
    protected _excute(): void;
}
/**
 * title 标题
 * cc 抄送
 * cc 密送
 * mailTo 收件人
 * mailText 正文
 * showTplContent 正文预览报表内容
 */
export interface MailLinkProps extends HyperLinkProps {
    title: string;
    cc: string;
    bcc: string;
    mailTo: string;
    mailText: string;
    showTplContent: boolean;
    parameters: string;
}
