export class AbstractLink {
    constructor(props, ..._external) {
        // 是否静谧触发，即多个时不出现下拉框
        this.silent = false;
        const { name } = props;
        this.name = name;
        this.init(props, _external);
    }
    init(_props, ..._external) { }
    excute() {
        this._excute();
    }
    getName() {
        return this.name;
    }
}
//# sourceMappingURL=abstract.link.js.map