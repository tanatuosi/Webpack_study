import { AbstractTarget } from './target.abstract';
export class DefaultTarget extends AbstractTarget {
    constructor(target, url, op) {
        super(target, url, op);
        this.options = op;
    }
    handle() {
        return window.open(this.url, '_self');
    }
}
//# sourceMappingURL=target.default.js.map