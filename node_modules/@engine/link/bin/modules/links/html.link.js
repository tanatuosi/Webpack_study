import { AbstractLink } from './abstract.link';
import { openUrl } from '../hyper_link.service';
import { appendParams2Url } from '@engine/crud';
export class HTMLLink extends AbstractLink {
    constructor(props) {
        super(props);
        const { targetFrame, url, width, height, parameters = [] } = props;
        this.url = url;
        this.target = targetFrame;
        this.width = width;
        this.height = height;
        this.parameters = parameters;
    }
    _excute() {
        openUrl({
            target: this.target,
            url: this.url,
            width: this.width,
            height: this.height,
            title: this.name,
        });
    }
    get url() {
        const parameter = {};
        this.parameters.forEach(item => {
            parameter[item.name] = item.value;
        });
        return appendParams2Url(this._url, parameter);
    }
    set url(url) {
        this._url = url;
    }
}
//# sourceMappingURL=html.link.js.map