import { requestGet, requestPost } from '@engine/crud';
/**
 * 获取超链接图表资源数据
 * @param ChartHyperlink_ID 超链接图表ID
 */
export function fetchChartPop(ChartHyperlink_ID, parameters) {
    return requestGet('chart/pop', {
        params: {
            ChartHyperlink_ID,
            __parameters__: parameters,
        },
    });
}
/**
 * 获取公式计算的值
 */
export async function remoteEvaluate(expression) {
    return requestPost('/remote/evaluate', {
        data: {
            expression,
        },
    });
}
/**
 * 发送邮件
 */
export function sendEmail(data) {
    return requestPost('email/send', {
        data,
    });
}
//# sourceMappingURL=hyper_link.crud.js.map