import { Type } from '@engine/core';
import { AxiosResponse } from 'axios';
import { AbstractErrorHandler } from './error_handler.abstract';
/**
 * 对外使用的provider
 */
declare class ErrorHandlerProvider {
    static xtype: string;
    private errorHandlerDict;
    $get(): {
        new (): {
            getHandler(key: string): AbstractErrorHandler;
            setHandler(key: string, value: Type<AbstractErrorHandler>): void;
            handle(response: AxiosResponse): AxiosResponse;
        };
    };
    addErrorHandler(type: string, value: {
        [key: string]: any;
    }): void;
}
export declare const errorHandlerProvider: InstanceType<ReturnType<ErrorHandlerProvider['$get']>>;
export {};
