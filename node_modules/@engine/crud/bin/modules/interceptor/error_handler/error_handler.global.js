import { ErrorTip } from '@engine/core';
import { AbstractErrorHandler } from './error_handler.abstract';
export class GlobalErrorHandler extends AbstractErrorHandler {
    handle(data) {
        const id = BI.UUID();
        const layer = BI.Layers.create(id);
        BI.createWidget({
            type: BI.CenterAdaptLayout.xtype,
            cls: 'bi-card',
            element: layer,
            items: [Object.assign({ type: ErrorTip.xtype }, data)],
        });
        BI.Layers.show(id);
    }
}
//# sourceMappingURL=error_handler.global.js.map